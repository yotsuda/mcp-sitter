name: CI

# Build + test on PR and push. The release workflow (release.yml) handles
# signing and publishing on tag pushes; this file is kept lean and scoped
# to correctness gating.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: test (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '9.0.x'

      - name: Build mcp-sitter (Debug)
        run: dotnet build -c Debug --nologo

      - name: Build FakeMcp (Debug)
        run: dotnet build test/FakeMcp/FakeMcp.csproj -c Debug --nologo

      - name: Install Pester 5
        shell: pwsh
        run: |
          if (-not (Get-Module -ListAvailable -Name Pester |
                    Where-Object { $_.Version -ge [Version]'5.0.0' })) {
            Install-Module Pester -MinimumVersion 5.0.0 -Force `
              -Scope CurrentUser -SkipPublisherCheck
          }

      - name: Run Pester integration tests
        shell: pwsh
        run: |
          Import-Module Pester -MinimumVersion 5.0.0 -Force
          $cfg = New-PesterConfiguration
          $cfg.Run.Path = './test/McpSitter.Tests.ps1'
          $cfg.Run.Exit = $true
          $cfg.Output.Verbosity = 'Detailed'
          Invoke-Pester -Configuration $cfg
